// here can define all events handler

// connection events
const CONNECTION = "connection";
const DISCONNECT = "disconnect";

// here export all events
export { CONNECTION, DISCONNECT };
console.log('Events constant handler is worked successfully');
